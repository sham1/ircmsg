// This code is autogenerated by split_test.py

#include <stdarg.h>
#include <stddef.h>
#include <stdlib.h>
#include <setjmp.h>
#include <cmocka.h>
#include <string.h>
#include <stdbool.h>
#include <ircmsg/parser.h>
#include <stdio.h>
#include "../parser_test.h"

static int
success_setup (void **state)
{{
	struct irc_test *test_struct = calloc(1, sizeof(*test_struct));
	if (test_struct == NULL) {{
		return -1;
	}}
	*state = test_struct;
	return 0;
}}

static int
success_teardown (void **state)
{{
	struct irc_test *test_struct = *state;
	free_msg(test_struct->msg);
	free(test_struct);
	return 0;
}}

{funcs}

int
main (int argc, char **argv)
{{
        const struct CMUnitTest tests[] = {{
		    {func_setups}
	}};

	return cmocka_run_group_tests_name("split_test", tests, NULL, NULL);
}}
